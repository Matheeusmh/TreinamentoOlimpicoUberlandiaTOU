#include <bits/stdc++.h>

using namespace std;
#define f first
#define s second
int main(void) {
  vector<pair<char, int>> tabela(5);
  
  for(int i = 0; i < 5; i++) cin >> tabela[i];
  
  tabela[0].first = p;
  tabela[1].first = v;
  tabela[2].first = e;
  tabela[3].first = d;
  tabela[4].first = j;
  
  for(auto e : tabela) {
  }
}
